describe("getFormattedAge", () => {
	it("should return the days, if the age is less than a month", () => {});
	it("should return the months, if the age is less than a year", () => {});
	it("should return the years and months, if the age is bigger than a year", () => {});
});
